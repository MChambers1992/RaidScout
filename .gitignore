# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.suo
*.user

# Claude Code (keep .claude/settings.json but ignore local overrides)
.claude/settings.local.json

# Chrome extension packaging output
*.zip
*.crx
*.pem

# Node (if tooling is ever added)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secrets / env (never commit these)
.env
.env.*
*.key
*.pem
secrets.*
